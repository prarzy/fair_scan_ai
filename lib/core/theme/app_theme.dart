import 'package:flutter/material.dart';

class AppColors {
  // This is the one your Scaffold is looking for
  static const background = Color(0xFF0D1117);    // Deep Midnight
  
  static const primaryBrand = Color(0xFF0D1117);   
  static const cardBackground = Color(0xFF161B22); // Graphite/Grey for cards
  static const accentNeon = Color(0xFF2ECC71);     // Neon Mint
  static const secondaryBrand = Color(0xFF8B949E); // Muted Slate text
  static const danger = Color(0xFFFF4560);         // Cyber Red
  static const warning = Color(0xFFFEB019);        // Amber
  static const border = Color(0xFF30363D);         // Dark Grey borders
}